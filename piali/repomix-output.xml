This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitattributes
.gitignore
.mvn/wrapper/maven-wrapper.properties
docker-compose.yml
mvnw
mvnw.cmd
pom.xml
src/main/java/com/jerrycf/piali/config/ClaudinaryConfig.java
src/main/java/com/jerrycf/piali/config/CorsConfig.java
src/main/java/com/jerrycf/piali/config/DataInitializer.java
src/main/java/com/jerrycf/piali/config/PasswordEncoderConfig.java
src/main/java/com/jerrycf/piali/controller/AuthController.java
src/main/java/com/jerrycf/piali/controller/DestinationController.java
src/main/java/com/jerrycf/piali/controller/TourController.java
src/main/java/com/jerrycf/piali/controller/UserController.java
src/main/java/com/jerrycf/piali/exception/EmailAlreadyExistsException.java
src/main/java/com/jerrycf/piali/exception/GlobalExceptionHandler.java
src/main/java/com/jerrycf/piali/exception/InvalidTokenException.java
src/main/java/com/jerrycf/piali/exception/ResourceNotFoundException.java
src/main/java/com/jerrycf/piali/model/DTO/destination/DestinationImageDTO.java
src/main/java/com/jerrycf/piali/model/DTO/destination/DestinationRequest.java
src/main/java/com/jerrycf/piali/model/DTO/destination/DestinationResponse.java
src/main/java/com/jerrycf/piali/model/DTO/errors/ErrorResponse.java
src/main/java/com/jerrycf/piali/model/DTO/tour/TourAvailabilityRequest.java
src/main/java/com/jerrycf/piali/model/DTO/tour/TourFeaturedRequest.java
src/main/java/com/jerrycf/piali/model/DTO/tour/TourRequest.java
src/main/java/com/jerrycf/piali/model/DTO/tour/TourResponse.java
src/main/java/com/jerrycf/piali/model/DTO/users/AuthResponse.java
src/main/java/com/jerrycf/piali/model/DTO/users/ChangePasswordRequest.java
src/main/java/com/jerrycf/piali/model/DTO/users/LoginRequest.java
src/main/java/com/jerrycf/piali/model/DTO/users/RefreshRequest.java
src/main/java/com/jerrycf/piali/model/DTO/users/RegisterRequest.java
src/main/java/com/jerrycf/piali/model/DTO/users/UpdateUserRequest.java
src/main/java/com/jerrycf/piali/model/DTO/users/UserResponse.java
src/main/java/com/jerrycf/piali/model/entity/Destination.java
src/main/java/com/jerrycf/piali/model/entity/DifficultyLevel.java
src/main/java/com/jerrycf/piali/model/entity/MexicanState.java
src/main/java/com/jerrycf/piali/model/entity/Role.java
src/main/java/com/jerrycf/piali/model/entity/Tour.java
src/main/java/com/jerrycf/piali/model/entity/TourType.java
src/main/java/com/jerrycf/piali/model/entity/TransportType.java
src/main/java/com/jerrycf/piali/model/entity/User.java
src/main/java/com/jerrycf/piali/PialiApplication.java
src/main/java/com/jerrycf/piali/repository/DestinationRepository.java
src/main/java/com/jerrycf/piali/repository/TourRepository.java
src/main/java/com/jerrycf/piali/repository/UserRepository.java
src/main/java/com/jerrycf/piali/security/JwtAuthFilter.java
src/main/java/com/jerrycf/piali/security/JwtService.java
src/main/java/com/jerrycf/piali/security/SecurityConfig.java
src/main/java/com/jerrycf/piali/service/AuthService.java
src/main/java/com/jerrycf/piali/service/DestinationService.java
src/main/java/com/jerrycf/piali/service/TourService.java
src/main/java/com/jerrycf/piali/service/UserService.java
src/main/resources/application.properties
src/main/resources/db/migration/V2__update_description_to_text.sql
src/test/java/com/jerrycf/piali/PialiApplicationTests.java
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".gitattributes">
/mvnw text eol=lf
*.cmd text eol=crlf
</file>

<file path=".gitignore">
# Spring Boot
HELP.md
target/
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

# IDE - STS
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

# IDE - IntelliJ IDEA
.idea/
*.iws
*.iml
*.ipr

# IDE - NetBeans
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

# IDE - VS Code
.vscode/

# Secretos y variables de entorno ← IMPORTANTE
.env
*.env
src/main/resources/application-local.properties
src/main/resources/application-secret.properties

# Logs
*.log
logs/
</file>

<file path=".mvn/wrapper/maven-wrapper.properties">
wrapperVersion=3.3.4
distributionType=only-script
distributionUrl=https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.14/apache-maven-3.9.14-bin.zip
</file>

<file path="docker-compose.yml">
services:
  db:
    container_name: piali-postgres-spring-boot
    image: postgres:latest
    environment:
      POSTGRES_USER: jerrycf
      POSTGRES_PASSWORD: password
      PGDATA: /data/postgres
    volumes:
      - db:/data/postgres
    ports:
      - "5373:5432"
    networks:
      - db
    restart: unless-stopped

networks:
  db:
    driver: bridge

volumes:
  db:
</file>

<file path="mvnw">
#!/bin/sh
# ----------------------------------------------------------------------------
# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# Apache Maven Wrapper startup batch script, version 3.3.4
#
# Optional ENV vars
# -----------------
#   JAVA_HOME - location of a JDK home dir, required when download maven via java source
#   MVNW_REPOURL - repo url base for downloading maven distribution
#   MVNW_USERNAME/MVNW_PASSWORD - user and password for downloading maven
#   MVNW_VERBOSE - true: enable verbose log; debug: trace the mvnw script; others: silence the output
# ----------------------------------------------------------------------------

set -euf
[ "${MVNW_VERBOSE-}" != debug ] || set -x

# OS specific support.
native_path() { printf %s\\n "$1"; }
case "$(uname)" in
CYGWIN* | MINGW*)
  [ -z "${JAVA_HOME-}" ] || JAVA_HOME="$(cygpath --unix "$JAVA_HOME")"
  native_path() { cygpath --path --windows "$1"; }
  ;;
esac

# set JAVACMD and JAVACCMD
set_java_home() {
  # For Cygwin and MinGW, ensure paths are in Unix format before anything is touched
  if [ -n "${JAVA_HOME-}" ]; then
    if [ -x "$JAVA_HOME/jre/sh/java" ]; then
      # IBM's JDK on AIX uses strange locations for the executables
      JAVACMD="$JAVA_HOME/jre/sh/java"
      JAVACCMD="$JAVA_HOME/jre/sh/javac"
    else
      JAVACMD="$JAVA_HOME/bin/java"
      JAVACCMD="$JAVA_HOME/bin/javac"

      if [ ! -x "$JAVACMD" ] || [ ! -x "$JAVACCMD" ]; then
        echo "The JAVA_HOME environment variable is not defined correctly, so mvnw cannot run." >&2
        echo "JAVA_HOME is set to \"$JAVA_HOME\", but \"\$JAVA_HOME/bin/java\" or \"\$JAVA_HOME/bin/javac\" does not exist." >&2
        return 1
      fi
    fi
  else
    JAVACMD="$(
      'set' +e
      'unset' -f command 2>/dev/null
      'command' -v java
    )" || :
    JAVACCMD="$(
      'set' +e
      'unset' -f command 2>/dev/null
      'command' -v javac
    )" || :

    if [ ! -x "${JAVACMD-}" ] || [ ! -x "${JAVACCMD-}" ]; then
      echo "The java/javac command does not exist in PATH nor is JAVA_HOME set, so mvnw cannot run." >&2
      return 1
    fi
  fi
}

# hash string like Java String::hashCode
hash_string() {
  str="${1:-}" h=0
  while [ -n "$str" ]; do
    char="${str%"${str#?}"}"
    h=$(((h * 31 + $(LC_CTYPE=C printf %d "'$char")) % 4294967296))
    str="${str#?}"
  done
  printf %x\\n $h
}

verbose() { :; }
[ "${MVNW_VERBOSE-}" != true ] || verbose() { printf %s\\n "${1-}"; }

die() {
  printf %s\\n "$1" >&2
  exit 1
}

trim() {
  # MWRAPPER-139:
  #   Trims trailing and leading whitespace, carriage returns, tabs, and linefeeds.
  #   Needed for removing poorly interpreted newline sequences when running in more
  #   exotic environments such as mingw bash on Windows.
  printf "%s" "${1}" | tr -d '[:space:]'
}

scriptDir="$(dirname "$0")"
scriptName="$(basename "$0")"

# parse distributionUrl and optional distributionSha256Sum, requires .mvn/wrapper/maven-wrapper.properties
while IFS="=" read -r key value; do
  case "${key-}" in
  distributionUrl) distributionUrl=$(trim "${value-}") ;;
  distributionSha256Sum) distributionSha256Sum=$(trim "${value-}") ;;
  esac
done <"$scriptDir/.mvn/wrapper/maven-wrapper.properties"
[ -n "${distributionUrl-}" ] || die "cannot read distributionUrl property in $scriptDir/.mvn/wrapper/maven-wrapper.properties"

case "${distributionUrl##*/}" in
maven-mvnd-*bin.*)
  MVN_CMD=mvnd.sh _MVNW_REPO_PATTERN=/maven/mvnd/
  case "${PROCESSOR_ARCHITECTURE-}${PROCESSOR_ARCHITEW6432-}:$(uname -a)" in
  *AMD64:CYGWIN* | *AMD64:MINGW*) distributionPlatform=windows-amd64 ;;
  :Darwin*x86_64) distributionPlatform=darwin-amd64 ;;
  :Darwin*arm64) distributionPlatform=darwin-aarch64 ;;
  :Linux*x86_64*) distributionPlatform=linux-amd64 ;;
  *)
    echo "Cannot detect native platform for mvnd on $(uname)-$(uname -m), use pure java version" >&2
    distributionPlatform=linux-amd64
    ;;
  esac
  distributionUrl="${distributionUrl%-bin.*}-$distributionPlatform.zip"
  ;;
maven-mvnd-*) MVN_CMD=mvnd.sh _MVNW_REPO_PATTERN=/maven/mvnd/ ;;
*) MVN_CMD="mvn${scriptName#mvnw}" _MVNW_REPO_PATTERN=/org/apache/maven/ ;;
esac

# apply MVNW_REPOURL and calculate MAVEN_HOME
# maven home pattern: ~/.m2/wrapper/dists/{apache-maven-<version>,maven-mvnd-<version>-<platform>}/<hash>
[ -z "${MVNW_REPOURL-}" ] || distributionUrl="$MVNW_REPOURL$_MVNW_REPO_PATTERN${distributionUrl#*"$_MVNW_REPO_PATTERN"}"
distributionUrlName="${distributionUrl##*/}"
distributionUrlNameMain="${distributionUrlName%.*}"
distributionUrlNameMain="${distributionUrlNameMain%-bin}"
MAVEN_USER_HOME="${MAVEN_USER_HOME:-${HOME}/.m2}"
MAVEN_HOME="${MAVEN_USER_HOME}/wrapper/dists/${distributionUrlNameMain-}/$(hash_string "$distributionUrl")"

exec_maven() {
  unset MVNW_VERBOSE MVNW_USERNAME MVNW_PASSWORD MVNW_REPOURL || :
  exec "$MAVEN_HOME/bin/$MVN_CMD" "$@" || die "cannot exec $MAVEN_HOME/bin/$MVN_CMD"
}

if [ -d "$MAVEN_HOME" ]; then
  verbose "found existing MAVEN_HOME at $MAVEN_HOME"
  exec_maven "$@"
fi

case "${distributionUrl-}" in
*?-bin.zip | *?maven-mvnd-?*-?*.zip) ;;
*) die "distributionUrl is not valid, must match *-bin.zip or maven-mvnd-*.zip, but found '${distributionUrl-}'" ;;
esac

# prepare tmp dir
if TMP_DOWNLOAD_DIR="$(mktemp -d)" && [ -d "$TMP_DOWNLOAD_DIR" ]; then
  clean() { rm -rf -- "$TMP_DOWNLOAD_DIR"; }
  trap clean HUP INT TERM EXIT
else
  die "cannot create temp dir"
fi

mkdir -p -- "${MAVEN_HOME%/*}"

# Download and Install Apache Maven
verbose "Couldn't find MAVEN_HOME, downloading and installing it ..."
verbose "Downloading from: $distributionUrl"
verbose "Downloading to: $TMP_DOWNLOAD_DIR/$distributionUrlName"

# select .zip or .tar.gz
if ! command -v unzip >/dev/null; then
  distributionUrl="${distributionUrl%.zip}.tar.gz"
  distributionUrlName="${distributionUrl##*/}"
fi

# verbose opt
__MVNW_QUIET_WGET=--quiet __MVNW_QUIET_CURL=--silent __MVNW_QUIET_UNZIP=-q __MVNW_QUIET_TAR=''
[ "${MVNW_VERBOSE-}" != true ] || __MVNW_QUIET_WGET='' __MVNW_QUIET_CURL='' __MVNW_QUIET_UNZIP='' __MVNW_QUIET_TAR=v

# normalize http auth
case "${MVNW_PASSWORD:+has-password}" in
'') MVNW_USERNAME='' MVNW_PASSWORD='' ;;
has-password) [ -n "${MVNW_USERNAME-}" ] || MVNW_USERNAME='' MVNW_PASSWORD='' ;;
esac

if [ -z "${MVNW_USERNAME-}" ] && command -v wget >/dev/null; then
  verbose "Found wget ... using wget"
  wget ${__MVNW_QUIET_WGET:+"$__MVNW_QUIET_WGET"} "$distributionUrl" -O "$TMP_DOWNLOAD_DIR/$distributionUrlName" || die "wget: Failed to fetch $distributionUrl"
elif [ -z "${MVNW_USERNAME-}" ] && command -v curl >/dev/null; then
  verbose "Found curl ... using curl"
  curl ${__MVNW_QUIET_CURL:+"$__MVNW_QUIET_CURL"} -f -L -o "$TMP_DOWNLOAD_DIR/$distributionUrlName" "$distributionUrl" || die "curl: Failed to fetch $distributionUrl"
elif set_java_home; then
  verbose "Falling back to use Java to download"
  javaSource="$TMP_DOWNLOAD_DIR/Downloader.java"
  targetZip="$TMP_DOWNLOAD_DIR/$distributionUrlName"
  cat >"$javaSource" <<-END
	public class Downloader extends java.net.Authenticator
	{
	  protected java.net.PasswordAuthentication getPasswordAuthentication()
	  {
	    return new java.net.PasswordAuthentication( System.getenv( "MVNW_USERNAME" ), System.getenv( "MVNW_PASSWORD" ).toCharArray() );
	  }
	  public static void main( String[] args ) throws Exception
	  {
	    setDefault( new Downloader() );
	    java.nio.file.Files.copy( java.net.URI.create( args[0] ).toURL().openStream(), java.nio.file.Paths.get( args[1] ).toAbsolutePath().normalize() );
	  }
	}
	END
  # For Cygwin/MinGW, switch paths to Windows format before running javac and java
  verbose " - Compiling Downloader.java ..."
  "$(native_path "$JAVACCMD")" "$(native_path "$javaSource")" || die "Failed to compile Downloader.java"
  verbose " - Running Downloader.java ..."
  "$(native_path "$JAVACMD")" -cp "$(native_path "$TMP_DOWNLOAD_DIR")" Downloader "$distributionUrl" "$(native_path "$targetZip")"
fi

# If specified, validate the SHA-256 sum of the Maven distribution zip file
if [ -n "${distributionSha256Sum-}" ]; then
  distributionSha256Result=false
  if [ "$MVN_CMD" = mvnd.sh ]; then
    echo "Checksum validation is not supported for maven-mvnd." >&2
    echo "Please disable validation by removing 'distributionSha256Sum' from your maven-wrapper.properties." >&2
    exit 1
  elif command -v sha256sum >/dev/null; then
    if echo "$distributionSha256Sum  $TMP_DOWNLOAD_DIR/$distributionUrlName" | sha256sum -c - >/dev/null 2>&1; then
      distributionSha256Result=true
    fi
  elif command -v shasum >/dev/null; then
    if echo "$distributionSha256Sum  $TMP_DOWNLOAD_DIR/$distributionUrlName" | shasum -a 256 -c >/dev/null 2>&1; then
      distributionSha256Result=true
    fi
  else
    echo "Checksum validation was requested but neither 'sha256sum' or 'shasum' are available." >&2
    echo "Please install either command, or disable validation by removing 'distributionSha256Sum' from your maven-wrapper.properties." >&2
    exit 1
  fi
  if [ $distributionSha256Result = false ]; then
    echo "Error: Failed to validate Maven distribution SHA-256, your Maven distribution might be compromised." >&2
    echo "If you updated your Maven version, you need to update the specified distributionSha256Sum property." >&2
    exit 1
  fi
fi

# unzip and move
if command -v unzip >/dev/null; then
  unzip ${__MVNW_QUIET_UNZIP:+"$__MVNW_QUIET_UNZIP"} "$TMP_DOWNLOAD_DIR/$distributionUrlName" -d "$TMP_DOWNLOAD_DIR" || die "failed to unzip"
else
  tar xzf${__MVNW_QUIET_TAR:+"$__MVNW_QUIET_TAR"} "$TMP_DOWNLOAD_DIR/$distributionUrlName" -C "$TMP_DOWNLOAD_DIR" || die "failed to untar"
fi

# Find the actual extracted directory name (handles snapshots where filename != directory name)
actualDistributionDir=""

# First try the expected directory name (for regular distributions)
if [ -d "$TMP_DOWNLOAD_DIR/$distributionUrlNameMain" ]; then
  if [ -f "$TMP_DOWNLOAD_DIR/$distributionUrlNameMain/bin/$MVN_CMD" ]; then
    actualDistributionDir="$distributionUrlNameMain"
  fi
fi

# If not found, search for any directory with the Maven executable (for snapshots)
if [ -z "$actualDistributionDir" ]; then
  # enable globbing to iterate over items
  set +f
  for dir in "$TMP_DOWNLOAD_DIR"/*; do
    if [ -d "$dir" ]; then
      if [ -f "$dir/bin/$MVN_CMD" ]; then
        actualDistributionDir="$(basename "$dir")"
        break
      fi
    fi
  done
  set -f
fi

if [ -z "$actualDistributionDir" ]; then
  verbose "Contents of $TMP_DOWNLOAD_DIR:"
  verbose "$(ls -la "$TMP_DOWNLOAD_DIR")"
  die "Could not find Maven distribution directory in extracted archive"
fi

verbose "Found extracted Maven distribution directory: $actualDistributionDir"
printf %s\\n "$distributionUrl" >"$TMP_DOWNLOAD_DIR/$actualDistributionDir/mvnw.url"
mv -- "$TMP_DOWNLOAD_DIR/$actualDistributionDir" "$MAVEN_HOME" || [ -d "$MAVEN_HOME" ] || die "fail to move MAVEN_HOME"

clean || :
exec_maven "$@"
</file>

<file path="mvnw.cmd">
<# : batch portion
@REM ----------------------------------------------------------------------------
@REM Licensed to the Apache Software Foundation (ASF) under one
@REM or more contributor license agreements.  See the NOTICE file
@REM distributed with this work for additional information
@REM regarding copyright ownership.  The ASF licenses this file
@REM to you under the Apache License, Version 2.0 (the
@REM "License"); you may not use this file except in compliance
@REM with the License.  You may obtain a copy of the License at
@REM
@REM    http://www.apache.org/licenses/LICENSE-2.0
@REM
@REM Unless required by applicable law or agreed to in writing,
@REM software distributed under the License is distributed on an
@REM "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
@REM KIND, either express or implied.  See the License for the
@REM specific language governing permissions and limitations
@REM under the License.
@REM ----------------------------------------------------------------------------

@REM ----------------------------------------------------------------------------
@REM Apache Maven Wrapper startup batch script, version 3.3.4
@REM
@REM Optional ENV vars
@REM   MVNW_REPOURL - repo url base for downloading maven distribution
@REM   MVNW_USERNAME/MVNW_PASSWORD - user and password for downloading maven
@REM   MVNW_VERBOSE - true: enable verbose log; others: silence the output
@REM ----------------------------------------------------------------------------

@IF "%__MVNW_ARG0_NAME__%"=="" (SET __MVNW_ARG0_NAME__=%~nx0)
@SET __MVNW_CMD__=
@SET __MVNW_ERROR__=
@SET __MVNW_PSMODULEP_SAVE=%PSModulePath%
@SET PSModulePath=
@FOR /F "usebackq tokens=1* delims==" %%A IN (`powershell -noprofile "& {$scriptDir='%~dp0'; $script='%__MVNW_ARG0_NAME__%'; icm -ScriptBlock ([Scriptblock]::Create((Get-Content -Raw '%~f0'))) -NoNewScope}"`) DO @(
  IF "%%A"=="MVN_CMD" (set __MVNW_CMD__=%%B) ELSE IF "%%B"=="" (echo %%A) ELSE (echo %%A=%%B)
)
@SET PSModulePath=%__MVNW_PSMODULEP_SAVE%
@SET __MVNW_PSMODULEP_SAVE=
@SET __MVNW_ARG0_NAME__=
@SET MVNW_USERNAME=
@SET MVNW_PASSWORD=
@IF NOT "%__MVNW_CMD__%"=="" ("%__MVNW_CMD__%" %*)
@echo Cannot start maven from wrapper >&2 && exit /b 1
@GOTO :EOF
: end batch / begin powershell #>

$ErrorActionPreference = "Stop"
if ($env:MVNW_VERBOSE -eq "true") {
  $VerbosePreference = "Continue"
}

# calculate distributionUrl, requires .mvn/wrapper/maven-wrapper.properties
$distributionUrl = (Get-Content -Raw "$scriptDir/.mvn/wrapper/maven-wrapper.properties" | ConvertFrom-StringData).distributionUrl
if (!$distributionUrl) {
  Write-Error "cannot read distributionUrl property in $scriptDir/.mvn/wrapper/maven-wrapper.properties"
}

switch -wildcard -casesensitive ( $($distributionUrl -replace '^.*/','') ) {
  "maven-mvnd-*" {
    $USE_MVND = $true
    $distributionUrl = $distributionUrl -replace '-bin\.[^.]*$',"-windows-amd64.zip"
    $MVN_CMD = "mvnd.cmd"
    break
  }
  default {
    $USE_MVND = $false
    $MVN_CMD = $script -replace '^mvnw','mvn'
    break
  }
}

# apply MVNW_REPOURL and calculate MAVEN_HOME
# maven home pattern: ~/.m2/wrapper/dists/{apache-maven-<version>,maven-mvnd-<version>-<platform>}/<hash>
if ($env:MVNW_REPOURL) {
  $MVNW_REPO_PATTERN = if ($USE_MVND -eq $False) { "/org/apache/maven/" } else { "/maven/mvnd/" }
  $distributionUrl = "$env:MVNW_REPOURL$MVNW_REPO_PATTERN$($distributionUrl -replace "^.*$MVNW_REPO_PATTERN",'')"
}
$distributionUrlName = $distributionUrl -replace '^.*/',''
$distributionUrlNameMain = $distributionUrlName -replace '\.[^.]*$','' -replace '-bin$',''

$MAVEN_M2_PATH = "$HOME/.m2"
if ($env:MAVEN_USER_HOME) {
  $MAVEN_M2_PATH = "$env:MAVEN_USER_HOME"
}

if (-not (Test-Path -Path $MAVEN_M2_PATH)) {
    New-Item -Path $MAVEN_M2_PATH -ItemType Directory | Out-Null
}

$MAVEN_WRAPPER_DISTS = $null
if ((Get-Item $MAVEN_M2_PATH).Target[0] -eq $null) {
  $MAVEN_WRAPPER_DISTS = "$MAVEN_M2_PATH/wrapper/dists"
} else {
  $MAVEN_WRAPPER_DISTS = (Get-Item $MAVEN_M2_PATH).Target[0] + "/wrapper/dists"
}

$MAVEN_HOME_PARENT = "$MAVEN_WRAPPER_DISTS/$distributionUrlNameMain"
$MAVEN_HOME_NAME = ([System.Security.Cryptography.SHA256]::Create().ComputeHash([byte[]][char[]]$distributionUrl) | ForEach-Object {$_.ToString("x2")}) -join ''
$MAVEN_HOME = "$MAVEN_HOME_PARENT/$MAVEN_HOME_NAME"

if (Test-Path -Path "$MAVEN_HOME" -PathType Container) {
  Write-Verbose "found existing MAVEN_HOME at $MAVEN_HOME"
  Write-Output "MVN_CMD=$MAVEN_HOME/bin/$MVN_CMD"
  exit $?
}

if (! $distributionUrlNameMain -or ($distributionUrlName -eq $distributionUrlNameMain)) {
  Write-Error "distributionUrl is not valid, must end with *-bin.zip, but found $distributionUrl"
}

# prepare tmp dir
$TMP_DOWNLOAD_DIR_HOLDER = New-TemporaryFile
$TMP_DOWNLOAD_DIR = New-Item -Itemtype Directory -Path "$TMP_DOWNLOAD_DIR_HOLDER.dir"
$TMP_DOWNLOAD_DIR_HOLDER.Delete() | Out-Null
trap {
  if ($TMP_DOWNLOAD_DIR.Exists) {
    try { Remove-Item $TMP_DOWNLOAD_DIR -Recurse -Force | Out-Null }
    catch { Write-Warning "Cannot remove $TMP_DOWNLOAD_DIR" }
  }
}

New-Item -Itemtype Directory -Path "$MAVEN_HOME_PARENT" -Force | Out-Null

# Download and Install Apache Maven
Write-Verbose "Couldn't find MAVEN_HOME, downloading and installing it ..."
Write-Verbose "Downloading from: $distributionUrl"
Write-Verbose "Downloading to: $TMP_DOWNLOAD_DIR/$distributionUrlName"

$webclient = New-Object System.Net.WebClient
if ($env:MVNW_USERNAME -and $env:MVNW_PASSWORD) {
  $webclient.Credentials = New-Object System.Net.NetworkCredential($env:MVNW_USERNAME, $env:MVNW_PASSWORD)
}
[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
$webclient.DownloadFile($distributionUrl, "$TMP_DOWNLOAD_DIR/$distributionUrlName") | Out-Null

# If specified, validate the SHA-256 sum of the Maven distribution zip file
$distributionSha256Sum = (Get-Content -Raw "$scriptDir/.mvn/wrapper/maven-wrapper.properties" | ConvertFrom-StringData).distributionSha256Sum
if ($distributionSha256Sum) {
  if ($USE_MVND) {
    Write-Error "Checksum validation is not supported for maven-mvnd. `nPlease disable validation by removing 'distributionSha256Sum' from your maven-wrapper.properties."
  }
  Import-Module $PSHOME\Modules\Microsoft.PowerShell.Utility -Function Get-FileHash
  if ((Get-FileHash "$TMP_DOWNLOAD_DIR/$distributionUrlName" -Algorithm SHA256).Hash.ToLower() -ne $distributionSha256Sum) {
    Write-Error "Error: Failed to validate Maven distribution SHA-256, your Maven distribution might be compromised. If you updated your Maven version, you need to update the specified distributionSha256Sum property."
  }
}

# unzip and move
Expand-Archive "$TMP_DOWNLOAD_DIR/$distributionUrlName" -DestinationPath "$TMP_DOWNLOAD_DIR" | Out-Null

# Find the actual extracted directory name (handles snapshots where filename != directory name)
$actualDistributionDir = ""

# First try the expected directory name (for regular distributions)
$expectedPath = Join-Path "$TMP_DOWNLOAD_DIR" "$distributionUrlNameMain"
$expectedMvnPath = Join-Path "$expectedPath" "bin/$MVN_CMD"
if ((Test-Path -Path $expectedPath -PathType Container) -and (Test-Path -Path $expectedMvnPath -PathType Leaf)) {
  $actualDistributionDir = $distributionUrlNameMain
}

# If not found, search for any directory with the Maven executable (for snapshots)
if (!$actualDistributionDir) {
  Get-ChildItem -Path "$TMP_DOWNLOAD_DIR" -Directory | ForEach-Object {
    $testPath = Join-Path $_.FullName "bin/$MVN_CMD"
    if (Test-Path -Path $testPath -PathType Leaf) {
      $actualDistributionDir = $_.Name
    }
  }
}

if (!$actualDistributionDir) {
  Write-Error "Could not find Maven distribution directory in extracted archive"
}

Write-Verbose "Found extracted Maven distribution directory: $actualDistributionDir"
Rename-Item -Path "$TMP_DOWNLOAD_DIR/$actualDistributionDir" -NewName $MAVEN_HOME_NAME | Out-Null
try {
  Move-Item -Path "$TMP_DOWNLOAD_DIR/$MAVEN_HOME_NAME" -Destination $MAVEN_HOME_PARENT | Out-Null
} catch {
  if (! (Test-Path -Path "$MAVEN_HOME" -PathType Container)) {
    Write-Error "fail to move MAVEN_HOME"
  }
} finally {
  try { Remove-Item $TMP_DOWNLOAD_DIR -Recurse -Force | Out-Null }
  catch { Write-Warning "Cannot remove $TMP_DOWNLOAD_DIR" }
}

Write-Output "MVN_CMD=$MAVEN_HOME/bin/$MVN_CMD"
</file>

<file path="pom.xml">
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.4.5</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.jerrycf</groupId>
	<artifactId>piali</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>com.cloudinary</groupId>
            <artifactId>cloudinary-http44</artifactId>
            <version>1.36.0</version>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.3</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.3</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.3</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
			</plugin>
		</plugins>
	</build>
	<repositories>
		<repository>
			<id>spring-snapshots</id>
			<name>Spring Snapshots</name>
			<url>https://repo.spring.io/snapshot</url>
			<releases>
				<enabled>false</enabled>
			</releases>
		</repository>
	</repositories>
	<pluginRepositories>
		<pluginRepository>
			<id>spring-snapshots</id>
			<name>Spring Snapshots</name>
			<url>https://repo.spring.io/snapshot</url>
			<releases>
				<enabled>false</enabled>
			</releases>
		</pluginRepository>
	</pluginRepositories>

</project>
</file>

<file path="src/main/java/com/jerrycf/piali/config/ClaudinaryConfig.java">
package com.jerrycf.piali.config;

import com.cloudinary.Cloudinary;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.Map;
@Configuration
public class ClaudinaryConfig {

    @Value("${cloudinary.cloud-name}")
    private String cloudName;

    @Value("${cloudinary.api-key}")
    private String apiKey;

    @Value("${cloudinary.api-secret}")
    private String apiSecret;

    @Bean
    public Cloudinary cloudinary() {
        return new Cloudinary(Map.of(
                "cloud_name", cloudName,
                "api_key",    apiKey,
                "api_secret", apiSecret
        ));
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/config/CorsConfig.java">
package com.jerrycf.piali.config;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.cors.CorsConfiguration;
import org.springframework.web.cors.CorsConfigurationSource;
import org.springframework.web.cors.UrlBasedCorsConfigurationSource;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

import java.util.List;
@Configuration
public class CorsConfig {

    @Value("${app.core.allowed-origins}")
    private List<String> allowedOrigins;

    @Bean
    public CorsConfigurationSource corsConfigurationSource() {
        CorsConfiguration config = new CorsConfiguration();

        config.setAllowedOrigins(allowedOrigins);
        config.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"));
        config.setAllowedHeaders(List.of("Authorization", "Content-Type", "Accept"));
        config.setExposedHeaders(List.of("Authorization"));
        config.setAllowCredentials(true);
        config.setMaxAge(3600L); // preflight cache for 1 hour

        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/api/**", config);
        return source;

    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/config/DataInitializer.java">
package com.jerrycf.piali.config;

import com.jerrycf.piali.model.entity.Role;
import com.jerrycf.piali.model.entity.User;
import com.jerrycf.piali.repository.UserRepository;
import lombok.RequiredArgsConstructor;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Component;

@Component
@RequiredArgsConstructor
public class DataInitializer implements ApplicationRunner {

    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;

    @Value("${admin.email}")
    private String adminEmail;

    @Value("${admin.password}")
    private String adminPassword;

    @Override
    public void run(ApplicationArguments args) {
        if (userRepository.findByEmail("admin@piali.com").isEmpty()) {
            User admin = new User();
            admin.setEmail(adminEmail);
            admin.setPassword(passwordEncoder.encode(adminPassword));
            admin.setFirstName("Admin");
            admin.setLastName("Piali");
            admin.setRole(Role.ADMIN);

            userRepository.save(admin);
            System.out.println("----- Admin creado: admin@piali.com / admin123 -----");
        }
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/config/PasswordEncoderConfig.java">
package com.jerrycf.piali.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;

@Configuration
public class PasswordEncoderConfig {

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/controller/AuthController.java">
package com.jerrycf.piali.controller;

import com.jerrycf.piali.model.DTO.users.AuthResponse;
import com.jerrycf.piali.model.DTO.users.LoginRequest;
import com.jerrycf.piali.model.DTO.users.RefreshRequest;
import com.jerrycf.piali.model.DTO.users.RegisterRequest;
import com.jerrycf.piali.service.AuthService;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/auth")
@RequiredArgsConstructor
public class AuthController {

    private final AuthService authService;

    @PostMapping("/register")
    public ResponseEntity<AuthResponse> register(@Valid @RequestBody RegisterRequest request){
        return ResponseEntity.status(HttpStatus.CREATED).body(authService.register(request));
    }

    @PostMapping("/login")
    public ResponseEntity<AuthResponse> login(@Valid @RequestBody LoginRequest request) {
        return ResponseEntity.ok(authService.login(request));
    }

    @PostMapping("/refresh")
    public ResponseEntity<AuthResponse> refresh(@RequestBody RefreshRequest request) {
        return ResponseEntity.ok(authService.refresh(request.refreshToken()));
    }

    @PostMapping("/logout")
    public ResponseEntity<Void> logout() {
        // Wtih JWT stateless, logout is handle by client deleting the token, if needed to invalidate tokens on server,
        // it will be needed a blacklist from Redis
        return ResponseEntity.noContent().build();
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/controller/DestinationController.java">
package com.jerrycf.piali.controller;

import com.jerrycf.piali.model.DTO.destination.DestinationImageDTO;
import com.jerrycf.piali.model.DTO.destination.DestinationRequest;
import com.jerrycf.piali.model.DTO.destination.DestinationResponse;
import com.jerrycf.piali.model.entity.Destination;
import com.jerrycf.piali.service.DestinationService;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/destinations")
@RequiredArgsConstructor
public class DestinationController {

    private final DestinationService destinationService;

    /*****  GET  *****/
    @GetMapping
    public ResponseEntity<List<DestinationResponse>> findAllDestinations() {
        return ResponseEntity.ok(destinationService.findAllDestinations());
    }

    @GetMapping("/{id}")
    public ResponseEntity<DestinationResponse> findDestinationById(@PathVariable Long id) {
        return ResponseEntity.ok(destinationService.findDestinationById(id));
    }

    @GetMapping("/search")
    public ResponseEntity<List<DestinationResponse>> findDestinationsBySearch(@RequestParam String name) {
        return ResponseEntity.ok(destinationService.findDestinationsByName(name));
    }

    @GetMapping("/featured")
    public ResponseEntity<List<DestinationResponse>> getAllFeaturedDestinations() {
        return ResponseEntity.ok(destinationService.getAllFeaturedDestinations());
    }

    /***** POST  *****/
    @PostMapping
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<DestinationResponse> createDestination(@Valid @RequestBody DestinationRequest request) {
        return ResponseEntity.status(HttpStatus.CREATED).body(destinationService.createNewDestination(request));
    }

    /***** PUT  *****/

    @PutMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<DestinationResponse> updateDestination(@PathVariable Long id, @Valid @RequestBody DestinationRequest request) {
        return ResponseEntity.ok(destinationService.updateDestination(id, request));
    }

    @PutMapping("/{id}/image")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<DestinationResponse> updateDestinationImage(@PathVariable Long id, @Valid @RequestBody DestinationImageDTO dto){
        return ResponseEntity.ok(destinationService.updateDestinationImage(id, dto));
    }

    /*****  DELETE  *****/
    @DeleteMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<Void> deleteDestination(@PathVariable Long id) {

        destinationService.deleteDestinationById(id);
        return ResponseEntity.noContent().build();
    }

    @DeleteMapping("/all")
    @PreAuthorize("hasRole('ADMIN')")
    public void deleteAllDestinations() {
        destinationService.deleteAllDestinations();
    }

}
</file>

<file path="src/main/java/com/jerrycf/piali/controller/TourController.java">
package com.jerrycf.piali.controller;

import com.jerrycf.piali.model.DTO.tour.TourAvailabilityRequest;
import com.jerrycf.piali.model.DTO.tour.TourFeaturedRequest;
import com.jerrycf.piali.model.DTO.tour.TourRequest;
import com.jerrycf.piali.model.DTO.tour.TourResponse;
import com.jerrycf.piali.model.entity.TourType;
import com.jerrycf.piali.service.TourService;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/tours")
@RequiredArgsConstructor
public class TourController {

    private final TourService tourService;

    /*****  GET  *****/
    @GetMapping
    public ResponseEntity<List<TourResponse>> findAllTours(){
        return ResponseEntity.ok(tourService.getAllTours());
    }

    @GetMapping("/{id}")
    public ResponseEntity<TourResponse> findTourById(@PathVariable Long id) {
        return ResponseEntity.ok(tourService.getTourById(id));
    }

    @GetMapping("/destination/{destinationId}")
    public ResponseEntity<List<TourResponse>> findToursByDestinationId(@PathVariable Long destinationId) {
        return ResponseEntity.ok(tourService.getToursByDestination(destinationId));
    }

    @GetMapping("/featured")
    public ResponseEntity<List<TourResponse>> getFeaturedTours() {
        return ResponseEntity.ok(tourService.getFeaturedTours());
    }

    @GetMapping("/type/{tourType}")
    public ResponseEntity<List<TourResponse>> getToursByTourType(@PathVariable TourType tourType){
        return ResponseEntity.ok(tourService.getToursByType(tourType));
    }

    @GetMapping("/available")
    public ResponseEntity<List<TourResponse>> getAllAvailableTours() {
        return ResponseEntity.ok(tourService.getAllAvailableTours());
    }

    /*****  POST  *****/
    @PostMapping
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<TourResponse> createNewTour(@Valid @RequestBody TourRequest tourRequest){
        return ResponseEntity.status(HttpStatus.CREATED).body(tourService.createNewTour(tourRequest));
    }

    /*****  PUT  *****/
    @PutMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<TourResponse> updateTourById(@PathVariable Long id, @Valid @RequestBody TourRequest tourRequest) {
        return ResponseEntity.ok(tourService.updateTourById(id, tourRequest));
    }

    /*****  PATCH  *****/
    @PatchMapping("/{id}/availability")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<TourResponse> updateTourAvailabilityById(@PathVariable Long id, @Valid @RequestBody TourAvailabilityRequest request) {
        return ResponseEntity.ok(tourService.updateTourAvailabilityById(id, request));
    }

    @PatchMapping("/{id}/featured")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<TourResponse> updateFeaturedTourById(@PathVariable Long id, @Valid @RequestBody TourFeaturedRequest request) {
        return ResponseEntity.ok(tourService.updateTourFeaturedById(id,request));
    }

    /*****  DELETE  *****/
    @DeleteMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<Void> deleteTourById(@PathVariable Long id) {
        tourService.deleteTourById(id);
        return ResponseEntity.noContent().build();
    }

}
</file>

<file path="src/main/java/com/jerrycf/piali/controller/UserController.java">
package com.jerrycf.piali.controller;

import com.jerrycf.piali.model.DTO.users.ChangePasswordRequest;
import com.jerrycf.piali.model.DTO.users.UpdateUserRequest;
import com.jerrycf.piali.model.DTO.users.UserResponse;
import com.jerrycf.piali.service.UserService;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/users")
@RequiredArgsConstructor
public class UserController {

    private final UserService userService;

    private String currentEmail() {
        return SecurityContextHolder.getContext().getAuthentication().getName();
    }

    @GetMapping
    public ResponseEntity<List<UserResponse>> getAllUsers() {
        return ResponseEntity.ok(userService.getAllUsers().stream()
                .map(UserResponse::from).toList());
    }

    @GetMapping("/me")
    public ResponseEntity<UserResponse> getMe() {
        return ResponseEntity.ok(UserResponse.from(userService.getMe(currentEmail())));
    }

    @PutMapping("/me")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<UserResponse> updateMe(@Valid @RequestBody UpdateUserRequest request) {
        return ResponseEntity.ok(UserResponse.from(userService.updateMe(currentEmail(), request)));
    }

    @PutMapping("/me/password")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<Void> changePassword(@Valid @RequestBody ChangePasswordRequest request) {
        userService.changePassword(currentEmail(), request);
        return ResponseEntity.noContent().build();
    }

    @DeleteMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<Void> deleteUser(@PathVariable Long id){
        userService.deleteUserById(id);
        return ResponseEntity.noContent().build();
    }



}
</file>

<file path="src/main/java/com/jerrycf/piali/exception/EmailAlreadyExistsException.java">
package com.jerrycf.piali.exception;

public class EmailAlreadyExistsException extends RuntimeException{
    public EmailAlreadyExistsException(String email){
        super("Email already registered: " + email);
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/exception/GlobalExceptionHandler.java">
package com.jerrycf.piali.exception;

import com.jerrycf.piali.model.DTO.errors.ErrorResponse;
import io.jsonwebtoken.JwtException;
import jakarta.servlet.http.HttpServletRequest;
import lombok.extern.slf4j.Slf4j;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.security.access.AccessDeniedException;
import org.springframework.security.authentication.BadCredentialsException;

import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

import java.util.stream.Collectors;

@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {

    @ExceptionHandler(ResourceNotFoundException.class)
    public ResponseEntity<ErrorResponse> handleNotFound(ResourceNotFoundException ex, HttpServletRequest request) {
        return build(HttpStatus.NOT_FOUND, ex.getMessage(), request);
    }

    @ExceptionHandler(EmailAlreadyExistsException.class)
    public ResponseEntity<ErrorResponse> handleEmailExists(
            EmailAlreadyExistsException ex, HttpServletRequest request) {
        return build(HttpStatus.CONFLICT, ex.getMessage(), request);
    }

    @ExceptionHandler(InvalidTokenException.class)
    public ResponseEntity<ErrorResponse> handleInvalidToken(
            InvalidTokenException ex, HttpServletRequest request) {
        return build(HttpStatus.UNAUTHORIZED, ex.getMessage(), request);
    }

    // ── Spring Security ──────────────────────────────────────────────────

    @ExceptionHandler(BadCredentialsException.class)
    public ResponseEntity<ErrorResponse> handleBadCredentials(
            BadCredentialsException ex, HttpServletRequest request) {
        // Mensaje genérico intencional: no revela si el email existe o no
        return build(HttpStatus.UNAUTHORIZED, "Credenciales incorrectas", request);
    }

    @ExceptionHandler(AccessDeniedException.class)
    public ResponseEntity<ErrorResponse> handleAccessDenied(
            AccessDeniedException ex, HttpServletRequest request) {
        return build(HttpStatus.FORBIDDEN, "No tienes permisos para esta acción", request);
    }

    // ── Validaciones (@Valid) ────────────────────────────────────────────

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<ErrorResponse> handleValidation(
            MethodArgumentNotValidException ex, HttpServletRequest request) {

        // Recolecta todos los errores de campo en un solo mensaje
        String message = ex.getBindingResult()
                .getFieldErrors()
                .stream()
                .map(err -> err.getField() + ": " + err.getDefaultMessage())
                .collect(Collectors.joining(", "));

        return build(HttpStatus.BAD_REQUEST, message, request);
    }

    // ── JWT malformado o expirado ────────────────────────────────────────

    @ExceptionHandler(JwtException.class)
    public ResponseEntity<ErrorResponse> handleJwtException(
            JwtException ex, HttpServletRequest request) {
        log.warn("JWT inválido: {}", ex.getMessage());
        return build(HttpStatus.UNAUTHORIZED, "Token inválido o expirado", request);
    }

    // ── Fallback: cualquier excepción no manejada ────────────────────────

    @ExceptionHandler(Exception.class)
    public ResponseEntity<ErrorResponse> handleGeneral(
            Exception ex, HttpServletRequest request) {
        log.error("Error inesperado: ", ex);   // Log completo solo en servidor
        return build(HttpStatus.INTERNAL_SERVER_ERROR, "Error interno del servidor", request);
    }

    // ── Helper ───────────────────────────────────────────────────────────

    private ResponseEntity<ErrorResponse> build(
            HttpStatus status, String message, HttpServletRequest request) {
        return ResponseEntity.status(status).body(
                ErrorResponse.of(status.value(), status.getReasonPhrase(),
                        message, request.getRequestURI())
        );
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/exception/InvalidTokenException.java">
package com.jerrycf.piali.exception;

public class InvalidTokenException extends RuntimeException{
    public InvalidTokenException(String message){
        super(message);
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/exception/ResourceNotFoundException.java">
package com.jerrycf.piali.exception;

public class ResourceNotFoundException extends RuntimeException {
    public ResourceNotFoundException(String message){
        super(message);
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/destination/DestinationImageDTO.java">
package com.jerrycf.piali.model.DTO.destination;

import jakarta.validation.constraints.NotBlank;

public record DestinationImageDTO(@NotBlank String imageUrl) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/destination/DestinationRequest.java">
package com.jerrycf.piali.model.DTO.destination;

import com.jerrycf.piali.model.entity.MexicanState;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.PositiveOrZero;

public record DestinationRequest(

        @NotBlank(message = "El nombre es obligatorio")
        String name,

        @NotNull(message = "El estado es obligatorio")
        MexicanState state,

        @PositiveOrZero
        int distanceKmFromCDMX,

        String description,

        String imageUrl,

        @PositiveOrZero
        Double basePrice,

        Integer featuredOrder
)
{ }
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/destination/DestinationResponse.java">
package com.jerrycf.piali.model.DTO.destination;

import com.jerrycf.piali.model.entity.Destination;

import java.time.LocalDateTime;

public record DestinationResponse(

        Long id,
        String name,
        String state,
        int distanceKmFromCDMX,
        String description,
        String imageUrl,
        Double basePrice,
        Integer featuredOrder,
        LocalDateTime createdAt
) {

    public static DestinationResponse from(Destination destination) {
        return new DestinationResponse(
                destination.getId(),
                destination.getName(),
                destination.getState().name(),
                destination.getDistanceKmFromCDMX(),
                destination.getDescription(),
                destination.getImageUrl(),
                destination.getBasePrice(),
                destination.getFeaturedOrder(),
                destination.getCreatedAt()
        );
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/errors/ErrorResponse.java">
package com.jerrycf.piali.model.DTO.errors;

import java.time.LocalDateTime;

public record ErrorResponse(
        int status,
        String error,
        String message,
        String path,
        LocalDateTime timestamp
) {
    public static ErrorResponse of(int status, String error, String message, String path) {
        return new ErrorResponse(status, error, message, path, LocalDateTime.now());
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/tour/TourAvailabilityRequest.java">
package com.jerrycf.piali.model.DTO.tour;

import jakarta.validation.constraints.NotNull;

public record TourAvailabilityRequest(
        @NotNull(message = "La campo de disponibilidad es obligatorio")
        Boolean available
) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/tour/TourFeaturedRequest.java">
package com.jerrycf.piali.model.DTO.tour;

import jakarta.validation.constraints.NotNull;

public record TourFeaturedRequest(
        @NotNull Boolean featured
) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/tour/TourRequest.java">
package com.jerrycf.piali.model.DTO.tour;

import com.jerrycf.piali.model.entity.DifficultyLevel;
import com.jerrycf.piali.model.entity.TourType;
import com.jerrycf.piali.model.entity.TransportType;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Positive;
import jakarta.validation.constraints.PositiveOrZero;

public record TourRequest(

        @NotNull(message = "El destino es obligatorio")
        Long destinationId,

        @NotBlank(message = "El nombre es obligatorio")
        String name,
        
        String description,

        String imageUrl,
        
        @NotNull
        TourType tourType,
        
        @NotNull
        DifficultyLevel difficultyLevel,
        
        @Positive(message = "El precio por persona debe ser mayor a cero")
        Double pricePerPerson,
        
        @PositiveOrZero(message = "El precio para niños no puede ser negativo")
        Double childPrice,
        
        @PositiveOrZero(message = "La duracion no puede ser negativa")
        Integer durationDays,
        
        @Positive(message = "Las noches deben ser mayor a cero")
        Integer nights,
        
        @Positive(message = "La capacidad mínima debe ser mayor a cero")
        Integer minGroupSize,
        
        @Positive(message = "La capacidad máxima debe ser mayor a cero")
        Integer maxGroupSize,
        
        @NotBlank(message = "El punto de salida es obligatorio")
        String departurePoint,
        
        TransportType transportType,
        
        String includes,
        
        String notIncludes,
        
        String itinerary
) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/tour/TourResponse.java">
package com.jerrycf.piali.model.DTO.tour;

import com.jerrycf.piali.model.DTO.destination.DestinationResponse;
import com.jerrycf.piali.model.entity.*;

import java.time.LocalDateTime;

public record TourResponse(
        Long id,
        DestinationResponse destination,
        String name,
        String description,
        String imageUrl,
        TourType tourType,
        DifficultyLevel difficultyLevel,
        Double pricePerPerson,
        Double childPrice,
        Integer durationDays,
        Integer nights,
        Integer minGroupSize,
        Integer maxGroupSize,
        String departurePoint,
        TransportType transportType,
        String includes,
        String notIncludes,
        String itinerary,
        Boolean available,
        Boolean featured,
        LocalDateTime createdAt,
        LocalDateTime updatedAt
) {

    public static TourResponse from(Tour tour) {

        return new  TourResponse(
                tour.getId(),
                DestinationResponse.from(tour.getDestination()),
                tour.getName(),
                tour.getDescription(),
                tour.getImageUrl(),
                tour.getTourType(),
                tour.getDifficultyLevel(),
                tour.getPricePerPerson(),
                tour.getChildPrice(),
                tour.getDurationDays(),
                tour.getNights(),
                tour.getMinGroupSize(),
                tour.getMaxGroupSize(),
                tour.getDeparturePoint(),
                tour.getTransportType(),
                tour.getIncludes(),
                tour.getNotIncludes(),
                tour.getItinerary(),
                tour.getAvailable(),
                tour.getFeatured(),
                tour.getCreatedAt(),
                tour.getUpdatedAt()
        );
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/users/AuthResponse.java">
package com.jerrycf.piali.model.DTO.users;

public record AuthResponse(String accessToken, String refreshToken) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/users/ChangePasswordRequest.java">
package com.jerrycf.piali.model.DTO.users;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Size;

public record ChangePasswordRequest(
        @NotBlank String currentPassword,
        @Size(min = 8) String newPassword
) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/users/LoginRequest.java">
package com.jerrycf.piali.model.DTO.users;

import jakarta.validation.constraints.Email;
import jakarta.validation.constraints.NotBlank;

public record LoginRequest(
        @Email @NotBlank String email,
        @NotBlank String password
) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/users/RefreshRequest.java">
package com.jerrycf.piali.model.DTO.users;

public record RefreshRequest(String refreshToken) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/users/RegisterRequest.java">
package com.jerrycf.piali.model.DTO.users;

import jakarta.validation.constraints.Email;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Size;

public record RegisterRequest(
        @NotBlank String firstName,
        @NotBlank String lastName,
        @Email @NotBlank String email,
        @Size(min = 8) String password
) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/users/UpdateUserRequest.java">
package com.jerrycf.piali.model.DTO.users;

import jakarta.validation.constraints.NotBlank;

public record UpdateUserRequest(@NotBlank String firstName, @NotBlank String lastName) {
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/DTO/users/UserResponse.java">
package com.jerrycf.piali.model.DTO.users;

import com.jerrycf.piali.model.entity.Role;
import com.jerrycf.piali.model.entity.User;

public record UserResponse(Long id, String email, String firstName, String lastName, Role role) {

    public static UserResponse from(User user){
        return new UserResponse(user.getId(), user.getEmail(), user.getFirstName(), user.getLastName(), user.getRole());
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/entity/Destination.java">
package com.jerrycf.piali.model.entity;

import jakarta.persistence.*;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.PositiveOrZero;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;

@Entity
@Table(name = "destination")
@AllArgsConstructor
@NoArgsConstructor
@Data
public class Destination {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false)
    @NotBlank(message = "El nombre es obligatorio")
    private String name;

    @Column(nullable = false)
    @Enumerated(EnumType.STRING)
    @NotNull(message = "El estado es obligatorio")
    private MexicanState state;

    @PositiveOrZero(message = "La distancia no puede ser negativa")
    private int distanceKmFromCDMX;

    @Column(columnDefinition = "TEXT")
    private String description;

    private String imageUrl;

    @PositiveOrZero(message = "El precio base no puede ser negativo")
    private Double basePrice;

    private Integer featuredOrder;

    @Column(nullable = false, updatable = false)
    private LocalDateTime createdAt = LocalDateTime.now();
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/entity/DifficultyLevel.java">
package com.jerrycf.piali.model.entity;

public enum DifficultyLevel {
    FACIL,
    MODERADO,
    DIFICIL
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/entity/MexicanState.java">
package com.jerrycf.piali.model.entity;

public enum MexicanState {
    AGUASCALIENTES,
    BAJA_CALIFORNIA,
    BAJA_CALIFORNIA_SUR,
    CAMPECHE,
    CHIAPAS,
    CHIHUAHUA,
    CIUDAD_DE_MEXICO,
    COAHUILA,
    COLIMA,
    DURANGO,
    ESTADO_DE_MEXICO,
    GUANAJUATO,
    GUERRERO,
    HIDALGO,
    JALISCO,
    MICHOACAN,
    MORELOS,
    NAYARIT,
    NUEVO_LEON,
    OAXACA,
    PUEBLA,
    QUERETARO,
    QUINTANA_ROO,
    SAN_LUIS_POTOSI,
    SINALOA,
    SONORA,
    TABASCO,
    TAMAULIPAS,
    TLAXCALA,
    VERACRUZ,
    YUCATAN,
    ZACATECAS
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/entity/Role.java">
package com.jerrycf.piali.model.entity;

public enum Role {
    USER,ADMIN
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/entity/Tour.java">
package com.jerrycf.piali.model.entity;

import jakarta.persistence.*;
import jakarta.validation.constraints.*;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.hibernate.annotations.CreationTimestamp;
import org.hibernate.annotations.Fetch;
import org.hibernate.annotations.UpdateTimestamp;

import java.time.LocalDateTime;

@Entity
@Table(name = "tour")
@AllArgsConstructor
@NoArgsConstructor
@Data
public class Tour {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    // Relación con Destination
    @ManyToOne(fetch = FetchType.LAZY, optional = false)
    @JoinColumn(name = "destination_id", nullable = false)
    @NotNull(message = "El destino es obligatorio")
    private Destination destination;

    // Información general
    @Column(nullable = false)
    @NotBlank(message = "El nombre es obligatorio")
    private String name;

    @Column(columnDefinition = "TEXT")
    private String description;

    private String imageUrl;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    @NotNull(message = "El tipo de tour es obligatorio")
    private TourType tourType;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    @NotNull(message = "El nivel de dificultad es obligatorio")
    private DifficultyLevel difficultyLevel; // FACIL(no requiere condición particular), MODERADO(preferible condicion para terrenos un poco complicados), DIFÍCIL(indispensable buena condición para actividades extremas)

    // Precios
    @Column(nullable = false)
    @Positive(message = "El precio por persona debe ser mayor a cero")
    private Double pricePerPerson;

    @PositiveOrZero(message = "El precio para niños no puede ser negativo")
    private Double childPrice; // null = mismo precio que un adulto

    // Logística
    @Column(nullable = false)
    @PositiveOrZero(message = "La duración no puede ser negativa")
    private Integer durationDays;

    @Positive(message = "Las noches deben ser mayor a cero")
    private Integer nights; // null = tour de un día (sin hospedaje)

    @Column(nullable = false)
    @Positive(message = "La capacidad mínima debe ser mayor a cero")
    private Integer minGroupSize;

    @Column(nullable = false)
    @Positive(message = "La capacidad máxima debe ser mayor a cero")
    private Integer maxGroupSize;

    // Logística de salida
    @Column(nullable = false)
    @NotBlank(message = "El punto de salida es obligatorio")
    private String departurePoint;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private TransportType transportType;

    // Inclusiones
    @Column(columnDefinition = "TEXT")
    private String includes; // Ej: "Transporte, hospedaje 2 noches, desayunos"

    @Column(columnDefinition = "TEXT")
    private String notIncludes; // Ej: "Comidas, propinas, gastos personales"

    @Column(columnDefinition = "TEXT")
    private String itinerary; // Descripción día a día

    // Disponibilidad
    @Column(nullable = false)
    private Boolean available = true;

    @Column(nullable = false)
    private Boolean featured = false;

    // Auditoría
    @CreationTimestamp
    @Column(nullable = false)
    private LocalDateTime createdAt = LocalDateTime.now();

    @UpdateTimestamp
    @Column(nullable = false)
    private LocalDateTime updatedAt;

    /*@PrePersist
    protected void onCreate() {
        this.updatedAt = LocalDateTime.now();
    }

    @PreUpdate
    protected void onUpdate() {
        this.updatedAt = LocalDateTime.now();
    }*/




}
</file>

<file path="src/main/java/com/jerrycf/piali/model/entity/TourType.java">
package com.jerrycf.piali.model.entity;

public enum TourType {
    CULTURAL,
    AVENTURA,
    GASTRONOMICO,
    NATURALEZA,
    HISTORICO,
    OTRO
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/entity/TransportType.java">
package com.jerrycf.piali.model.entity;

public enum TransportType {
    CARRO,
    VAN,
    AUTOBUS,
    AVION,
    MIXTO
}
</file>

<file path="src/main/java/com/jerrycf/piali/model/entity/User.java">
package com.jerrycf.piali.model.entity;

import jakarta.persistence.*;
import lombok.Data;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;
import org.springframework.security.core.GrantedAuthority;
import org.springframework.security.core.authority.SimpleGrantedAuthority;
import org.springframework.security.core.userdetails.UserDetails;

import java.time.LocalDateTime;
import java.util.Collection;
import java.util.List;

@Entity
@Table(name = "users")
@Getter
@Setter
@NoArgsConstructor
public class User implements UserDetails {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(unique = true, nullable = false)
    private String email;

    @Column(nullable = false)
    private String password;

    @Column(nullable = false)
    private String firstName;

    @Column(nullable = false)
    private String lastName;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private Role role = Role.USER;

    @Column(nullable = false, updatable = false)
    private LocalDateTime createdAt = LocalDateTime.now();

    @Override
    public Collection<? extends GrantedAuthority> getAuthorities() {
        return List.of(new SimpleGrantedAuthority("ROLE_" + role.name()));
    }

    @Override public String getUsername() { return email; }
    @Override public boolean isAccountNonExpired() { return true; }
    @Override public boolean isAccountNonLocked() { return true; }
    @Override public boolean isCredentialsNonExpired() { return true; }
    @Override public boolean isEnabled() { return true; }
}
</file>

<file path="src/main/java/com/jerrycf/piali/PialiApplication.java">
package com.jerrycf.piali;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class PialiApplication {

	public static void main(String[] args) {
		SpringApplication.run(PialiApplication.class, args);
	}

}
</file>

<file path="src/main/java/com/jerrycf/piali/repository/DestinationRepository.java">
package com.jerrycf.piali.repository;

import com.jerrycf.piali.model.entity.Destination;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface DestinationRepository extends JpaRepository<Destination, Long> {
    List<Destination> findByState(String state);

    List<Destination> findByNameContainingIgnoreCase(String name);

    // Devuelve solo los featured, ordenados
    List<Destination> findByFeaturedOrderIsNotNullOrderByFeaturedOrder();

    boolean existsByNameIgnoreCase(String name);
}
</file>

<file path="src/main/java/com/jerrycf/piali/repository/TourRepository.java">
package com.jerrycf.piali.repository;

import com.jerrycf.piali.model.entity.Tour;
import com.jerrycf.piali.model.entity.TourType;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;

public interface TourRepository extends JpaRepository<Tour, Long> {
    List<Tour> findByNameContainingIgnoreCase(String name);

    boolean existsByNameIgnoreCase(String name);

    List<Tour> findByDestinationId(Long destinationId);

    List<Tour> findByFeaturedTrue();

    List<Tour> findByTourType(TourType tourType);

    List<Tour> findByAvailableTrue();
}
</file>

<file path="src/main/java/com/jerrycf/piali/repository/UserRepository.java">
package com.jerrycf.piali.repository;

import com.jerrycf.piali.model.entity.User;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.Optional;

public interface UserRepository extends JpaRepository<User, Long> {

    Optional<User> findByEmail(String username);

    boolean existsByEmail(String email);
}
</file>

<file path="src/main/java/com/jerrycf/piali/security/JwtAuthFilter.java">
package com.jerrycf.piali.security;

import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.web.authentication.WebAuthenticationDetailsSource;
import org.springframework.stereotype.Component;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;

@Component
@RequiredArgsConstructor
public class JwtAuthFilter extends OncePerRequestFilter {
    private final JwtService jwtService;;
    private final UserDetailsService userDetailsService;

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain chain)
        throws ServletException, IOException {

        String authHeader = request.getHeader("Authorization");

        if (authHeader == null || !authHeader.startsWith("Bearer ")) {
            chain.doFilter(request, response);
            return;
        }

        String token = authHeader.substring(7);
        String email = jwtService.extractEmail(token);

        //Only authenticate if email exists and context still without authentication
        if (email != null && SecurityContextHolder.getContext().getAuthentication() == null) {
            UserDetails user = userDetailsService.loadUserByUsername(email);

            if (jwtService.isTokenValid(token, user)) {
                UsernamePasswordAuthenticationToken authToken = new UsernamePasswordAuthenticationToken(user, null, user.getAuthorities());
                authToken.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
                SecurityContextHolder.getContext().setAuthentication(authToken);
            }
        }

        chain.doFilter(request, response);
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/security/JwtService.java">
package com.jerrycf.piali.security;

import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.io.Decoders;
import io.jsonwebtoken.security.Keys;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.security.core.GrantedAuthority;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.stereotype.Service;

import javax.crypto.SecretKey;
import java.util.Date;
import java.util.function.Function;


@Service
public class JwtService {

    @Value("${jwt.secret}")
    private String secretKey;

    @Value("${jwt.expiration}")      // ej: 900000 (15 min)
    private long accessExpiration;

    @Value("${jwt.refresh-expiration}") // ej: 604800000 (7 días)
    private long refreshExpiration;

    private SecretKey getSignKey() {
        return Keys.hmacShaKeyFor(Decoders.BASE64.decode(secretKey));
    }

    public String generateAccessToken(UserDetails user) {
        return buildToken(user, accessExpiration);
    }

    public String generateRefreshToken(UserDetails user) {
        return buildToken(user, refreshExpiration);
    }

    private String buildToken(UserDetails user, long expiration) {
        return Jwts.builder()
                .subject(user.getUsername())
                .claim("roles", user.getAuthorities().stream()
                        .map(GrantedAuthority::getAuthority).toList())
                .issuedAt(new Date())
                .expiration(new Date(System.currentTimeMillis() + expiration))
                .signWith(getSignKey())
                .compact();
    }

    public String extractEmail(String token) {
        return extractClaim(token, Claims::getSubject);
    }

    public boolean isTokenValid(String token, UserDetails user) {
        return extractEmail(token).equals(user.getUsername()) && !isTokenExpired(token);
    }

    private boolean isTokenExpired(String token) {
        return extractClaim(token, Claims::getExpiration).before(new Date());
    }

    private <T> T extractClaim(String token, Function<Claims, T> resolver) {
        Claims claims = Jwts.parser()
                .verifyWith(getSignKey())
                .build()
                .parseSignedClaims(token)
                .getPayload();
        return resolver.apply(claims);
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/security/SecurityConfig.java">
package com.jerrycf.piali.security;

import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.http.HttpMethod;
import org.springframework.security.authentication.AuthenticationManager;
import org.springframework.security.authentication.AuthenticationProvider;
import org.springframework.security.authentication.dao.DaoAuthenticationProvider;
import org.springframework.security.config.annotation.authentication.configuration.AuthenticationConfiguration;
import org.springframework.security.config.annotation.method.configuration.EnableMethodSecurity;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;

@Configuration
@EnableWebSecurity
@EnableMethodSecurity
@RequiredArgsConstructor
public class SecurityConfig {

    private final JwtAuthFilter jwtAuthFilter;
    private final UserDetailsService userDetailsService;
    private final PasswordEncoder passwordEncoder;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
                .csrf(csrf -> csrf.disable())
                .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
                .authorizeHttpRequests(auth -> auth
                                //.anyRequest().permitAll()
                        .requestMatchers("/auth/**").permitAll()
                        .requestMatchers(HttpMethod.GET, "/destinations/**").permitAll()
                        .requestMatchers(HttpMethod.GET, "/tours/**").permitAll()
                        .requestMatchers(HttpMethod.GET, "/users/**").permitAll()
                        //.requestMatchers(HttpMethod.GET, "/api/v1/destinations/featured").permitAll()
                        .anyRequest().authenticated() //Any other request needs authentication
                )
                .authenticationProvider(authenticationProvider())
                .addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class)
                .exceptionHandling(ex -> ex
                        .authenticationEntryPoint((request, response, authException) -> {
                            response.setContentType("application/json");
                            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
                            response.getWriter().write("""
            {"status":401,"error":"Unauthorized","message":"Autenticación requerida"}
            """);
                        })
                        .accessDeniedHandler((request, response, accessDeniedException) -> {
                            response.setContentType("application/json");
                            response.setStatus(HttpServletResponse.SC_FORBIDDEN);
                            response.getWriter().write("""
            {"status":403,"error":"Forbidden","message":"No tienes permisos para esta acción"}
            """);
                        })
                );

        return http.build();
    }


    @Bean
    public AuthenticationProvider authenticationProvider() {
        DaoAuthenticationProvider provider = new DaoAuthenticationProvider();
        provider.setUserDetailsService(userDetailsService);
        provider.setPasswordEncoder(passwordEncoder);

        return provider;
    }

    @Bean
    public AuthenticationManager authenticationManager(AuthenticationConfiguration config) throws Exception {
        return config.getAuthenticationManager();
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/service/AuthService.java">
package com.jerrycf.piali.service;

import com.jerrycf.piali.exception.EmailAlreadyExistsException;
import com.jerrycf.piali.model.DTO.users.AuthResponse;
import com.jerrycf.piali.model.DTO.users.LoginRequest;
import com.jerrycf.piali.model.DTO.users.RegisterRequest;
import com.jerrycf.piali.model.entity.User;
import com.jerrycf.piali.repository.UserRepository;
import com.jerrycf.piali.security.JwtService;
import lombok.RequiredArgsConstructor;
import org.springframework.security.authentication.AuthenticationManager;
import org.springframework.security.authentication.BadCredentialsException;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class AuthService {

    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;
    private final JwtService jwtService;
    private final AuthenticationManager authManager;

    public AuthResponse register(RegisterRequest request) {
        if (userRepository.existsByEmail(request.email())) {
            throw new EmailAlreadyExistsException(request.email());
        }

        User user = new User();
        user.setEmail(request.email());
        user.setPassword(passwordEncoder.encode(request.password()));
        user.setFirstName(request.firstName());
        user.setLastName(request.lastName());
        userRepository.save(user);

        return buildTokens(user);
    }

    public AuthResponse login(LoginRequest request) {
        //Already throws exception if wrong credentials
        authManager.authenticate(new UsernamePasswordAuthenticationToken(request.email(), request.password()));
        User user = userRepository.findByEmail(request.email()).orElseThrow();
        return buildTokens(user);
    }

    public AuthResponse refresh(String refreshToken) {
        String email = jwtService.extractEmail(refreshToken);
        User user = userRepository.findByEmail(email).orElseThrow();
        if (!jwtService.isTokenValid(refreshToken, user)){
            throw new BadCredentialsException("Refresh Token invalid or expired");
        }

        return buildTokens(user);
    }

    public AuthResponse buildTokens(User user) {
        return new AuthResponse(jwtService.generateAccessToken(user), jwtService.generateRefreshToken(user));
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/service/DestinationService.java">
package com.jerrycf.piali.service;

import com.jerrycf.piali.exception.ResourceNotFoundException;
import com.jerrycf.piali.model.DTO.destination.DestinationImageDTO;
import com.jerrycf.piali.model.DTO.destination.DestinationRequest;
import com.jerrycf.piali.model.DTO.destination.DestinationResponse;
import com.jerrycf.piali.model.entity.Destination;
import com.jerrycf.piali.repository.DestinationRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;

import java.time.LocalDateTime;
import java.util.Comparator;
import java.util.List;
import java.util.NoSuchElementException;
import java.util.stream.Collectors;

@Service
@RequiredArgsConstructor
public class DestinationService {
    public final DestinationRepository destinationRepository;

    /*****  GET  *****/
    public List<DestinationResponse> findAllDestinations() {

        return destinationRepository.findAll().stream()
                .map(DestinationResponse::from)
                .toList();
    }

    public DestinationResponse findDestinationById(Long id) {
        //Manage error handling if not existing
        return destinationRepository.findById(id).map(DestinationResponse::from)
                .orElseThrow(() -> new ResourceNotFoundException("Destino con id " + id + " no encontrado"));
    }

    public List<DestinationResponse> findDestinationsByName(String name) {
        String cleanName = name.trim().toLowerCase();
        return destinationRepository.findByNameContainingIgnoreCase(cleanName).stream()
                .map(DestinationResponse::from)
                .toList();
    }

    public List<DestinationResponse> getAllFeaturedDestinations() {
        return destinationRepository.findByFeaturedOrderIsNotNullOrderByFeaturedOrder().stream()
                .map(DestinationResponse::from)
                .toList();
    }

    /*****  POST  *****/
    public DestinationResponse createNewDestination(DestinationRequest request) {
        if (destinationRepository.existsByNameIgnoreCase(request.name())){
            throw new IllegalArgumentException("Destino con nombre: " + request.name() + " ya fue creado anteriormente");
        }
        Destination destination = new Destination();
        updateDestinationFromRequest(destination,request);

        return DestinationResponse.from(destinationRepository.save(destination));
    }

    /*****  PUT  *****/
    public DestinationResponse updateDestination(Long id, DestinationRequest request) {
        Destination destination = destinationRepository.findById(id)
                .orElseThrow(() -> new ResourceNotFoundException("Destino con id " + id + " no encontrado para actualizar"));

        updateDestinationFromRequest(destination, request);

        return DestinationResponse.from(destinationRepository.save(destination));

    }

    public void updateDestinationFromRequest(Destination destination, DestinationRequest request){
        destination.setName(request.name());
        destination.setState(request.state());
        destination.setDescription(request.description());
        destination.setDistanceKmFromCDMX(request.distanceKmFromCDMX());
        destination.setBasePrice(request.basePrice());
        destination.setFeaturedOrder(request.featuredOrder());
        destination.setImageUrl(request.imageUrl());
    }

    public DestinationResponse updateDestinationImage(Long id, DestinationImageDTO dto) {
        Destination dest = destinationRepository.findById(id).orElseThrow(() -> new ResourceNotFoundException("Destino con id: " + id + " no encontrado para actualizar imagen"));

        dest.setImageUrl(dto.imageUrl());
        return DestinationResponse.from(destinationRepository.save(dest));
    }


    /*****  DELETE  *****/

    public void deleteDestinationById(Long id) {
        if (!destinationRepository.existsById(id)){
            throw new ResourceNotFoundException("Destino con id: " + id + " no encontrado para eliminar");
        }
        //TODO check for disabling instead of deleting
        destinationRepository.deleteById(id);
    }

    public void deleteAllDestinations() {
        destinationRepository.deleteAll();
    }

}
</file>

<file path="src/main/java/com/jerrycf/piali/service/TourService.java">
package com.jerrycf.piali.service;

import com.jerrycf.piali.exception.ResourceNotFoundException;
import com.jerrycf.piali.model.DTO.tour.TourAvailabilityRequest;
import com.jerrycf.piali.model.DTO.tour.TourFeaturedRequest;
import com.jerrycf.piali.model.DTO.tour.TourRequest;
import com.jerrycf.piali.model.DTO.tour.TourResponse;
import com.jerrycf.piali.model.entity.Destination;
import com.jerrycf.piali.model.entity.Tour;
import com.jerrycf.piali.model.entity.TourType;
import com.jerrycf.piali.repository.DestinationRepository;
import com.jerrycf.piali.repository.TourRepository;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Service
@RequiredArgsConstructor
public class TourService {

    private final TourRepository tourRepository;
    private final DestinationRepository destinationRepository;


    /*****  GET  *****/
    public List<TourResponse> getAllTours(){
        return tourRepository.findAll().stream()
                .map(TourResponse::from)
                .toList();
    }

    @Transactional(readOnly = true)
    public TourResponse getTourById(Long id) {
        return tourRepository.findById(id)
                .map(TourResponse::from)
                .orElseThrow(() -> new ResourceNotFoundException("Tour con id: " + id + " no encontrado."));
    }

    public List<TourResponse> getToursByDestination(Long destinationId) {
        if (!destinationRepository.existsById(destinationId)) {
            throw new  ResourceNotFoundException("Tour con Destination id: " + destinationId + " no encontrado.");
        }
        return tourRepository.findByDestinationId(destinationId).stream()
                .map(TourResponse::from)
                .toList();

    }

    public List<TourResponse> getFeaturedTours() {
        return tourRepository.findByFeaturedTrue().stream()
                .map(TourResponse::from)
                .toList();
    }

    public List<TourResponse> getToursByType(TourType tourType) {
        return tourRepository.findByTourType(tourType).stream()
                .map(TourResponse::from)
                .toList();
    }

    public List<TourResponse> getAllAvailableTours() {
        return tourRepository.findByAvailableTrue().stream()
                .map(TourResponse::from)
                .toList();
    }

    /***** POST  *****/
    public TourResponse createNewTour(TourRequest request) {
        if (tourRepository.existsByNameIgnoreCase(request.name())){
            throw new IllegalArgumentException("Tour ya existente con nombre: " + request.name());
        }

        Tour tour = new Tour();
        updateTourFromRequest(tour,request);

        return TourResponse.from(tourRepository.save(tour));
    }

    /***** PUT  *****/
    public TourResponse updateTourById(Long id, TourRequest request) {
        // Tour tour = tourRepository.findById(id)
        //        .orElseThrow(() -> new ResourceNotFoundException("Tour id: " + id + " no encontrado para actualizar."));
        Tour tour = getTour(id);

        updateTourFromRequest(tour, request);

        return TourResponse.from(tourRepository.save(tour));
    }


    /*****   PATCH  *****/
    public TourResponse updateTourAvailabilityById(Long id, TourAvailabilityRequest request) {
        //Tour tour = tourRepository.findById(id)
        //        .orElseThrow(() -> new ResourceNotFoundException("Tour id: " + id + " no encontrado para cambiar disponibilidad."));
        Tour tour = getTour(id);

        tour.setAvailable(request.available());
        return TourResponse.from(tourRepository.save(tour));
    }

    public TourResponse updateTourFeaturedById(Long id, TourFeaturedRequest request) {
        Tour tour = getTour(id);
        tour.setFeatured(request.featured());

        return TourResponse.from(tourRepository.save(tour));
    }

    /*****   DELETE  *****/
    public void deleteTourById(Long id) {
        if (!tourRepository.existsById(id)){
            throw new ResourceNotFoundException("Tour con id: " + id + " no encontrado para elminar.");
        }
        tourRepository.deleteById(id);
    }



    public Tour getTour(Long id) {
        return tourRepository.findById(id).orElseThrow(() -> new ResourceNotFoundException("Tour id: " + id + " no encontrado."));
    }


    public void updateTourFromRequest(Tour tour, TourRequest request) {
        tour.setName(request.name());
        tour.setDestination(destinationRepository.findById(request.destinationId())
                .orElseThrow(() -> new ResourceNotFoundException("destino con id " + request.destinationId() + " no encontrado.")));
        tour.setDescription(request.description());
        tour.setImageUrl(request.imageUrl());
        tour.setTourType(request.tourType());
        tour.setDifficultyLevel(request.difficultyLevel());
        tour.setPricePerPerson(request.pricePerPerson());
        tour.setChildPrice(request.childPrice());
        tour.setDurationDays(request.durationDays());
        tour.setNights(request.nights());
        tour.setMinGroupSize(request.minGroupSize());
        tour.setMaxGroupSize(request.maxGroupSize());
        tour.setDeparturePoint(request.departurePoint());
        tour.setTransportType(request.transportType());
        tour.setIncludes(request.includes());
        tour.setNotIncludes(request.notIncludes());
        tour.setItinerary(request.itinerary());
    }
}
</file>

<file path="src/main/java/com/jerrycf/piali/service/UserService.java">
package com.jerrycf.piali.service;


import com.jerrycf.piali.exception.ResourceNotFoundException;
import com.jerrycf.piali.model.DTO.users.ChangePasswordRequest;
import com.jerrycf.piali.model.DTO.users.UpdateUserRequest;
import com.jerrycf.piali.model.entity.User;
import com.jerrycf.piali.repository.UserRepository;
import jakarta.persistence.EntityNotFoundException;
import lombok.RequiredArgsConstructor;
import org.springframework.security.authentication.BadCredentialsException;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.core.userdetails.UsernameNotFoundException;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
@RequiredArgsConstructor
public class UserService implements UserDetailsService {

    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;

    @Override
    public UserDetails loadUserByUsername(String email) throws UsernameNotFoundException {
        return userRepository.findByEmail(email)
                .orElseThrow(() -> new UsernameNotFoundException("Usuario no encontrado: " + email));
    }

    public List<User> getAllUsers() {
        return userRepository.findAll();
    }

    public User getMe(String email) {
        return userRepository.findByEmail(email)
                .orElseThrow(() -> new ResourceNotFoundException("Usuario no encontrado"));
    }

    public User updateMe(String email, UpdateUserRequest request) {
        User user = getMe(email);
        user.setFirstName(request.firstName());
        user.setLastName(request.lastName());
        return userRepository.save(user);
    }

    public void changePassword(String email, ChangePasswordRequest request) {
        User user = getMe(email);
        if (!passwordEncoder.matches(request.currentPassword(), user.getPassword())) {
            throw new BadCredentialsException("Contraseña actual incorrecta");
        }
        user.setPassword(passwordEncoder.encode(request.newPassword()));
        userRepository.save(user);
    }

    public void deleteUserById(Long id) {
        if (!userRepository.existsById(id)) {
            throw new ResourceNotFoundException("Usuario no encontrado: " + id);
        }

        userRepository.deleteById(id);
    }

}
</file>

<file path="src/main/resources/application.properties">
# Aplicación
spring.application.name=piali
server.port=${PORT:8080}
server.servlet.context-path=/api/v1

# Base de datos
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# Jackson
spring.jackson.default-property-inclusion=non_null

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=900000
jwt.refresh-expiration=604800000

# Logging
logging.level.root=INFO
logging.level.com.tuempresa.piali=DEBUG

# CORS
app.core.allowed-origins=${ALLOWED_ORIGINS}

# Data Initializer
admin.email=${ADMIN_EMAIL}
admin.password=${ADMIN_PASSWORD}
</file>

<file path="src/main/resources/db/migration/V2__update_description_to_text.sql">
ALTER TABLE destination ALTER COLUMN description TYPE TEXT;
</file>

<file path="src/test/java/com/jerrycf/piali/PialiApplicationTests.java">
package com.jerrycf.piali;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class PialiApplicationTests {

	@Test
	void contextLoads() {
	}

}
</file>

</files>
